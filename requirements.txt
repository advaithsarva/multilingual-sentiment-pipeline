langdetect==1.0.9
pysbd==0.3.4
spacy==3.8.7
transformers>=4.40
torch>=2.0
# The sentiment model is XLM-RoBERTa, whose tokenizer is SentencePiece.
# transformers requires neither of these, needs both to convert that tokenizer,
# and on a clean install silently falls back to a TikToken extractor and then
# raises `tiktoken is required` -- taking out the three tests that load the
# model. Only CI sees it; any machine with another SentencePiece model has both
# already, which is why the first fix named only sentencepiece and CI came back
# asking for protobuf as well.
sentencepiece>=0.2
protobuf>=4.25

# after installing, fetch the models:
#   python -m spacy download en_core_web_sm
#   python -m spacy download xx_ent_wiki_sm
# the sentiment model (cardiffnlp/twitter-xlm-roberta-base-sentiment) downloads
# automatically on first run via `transformers` and is cached locally after.
